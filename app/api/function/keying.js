/* Iklan untuk lets dance*/
var connection = requier('../conn');

exports.getKey = function(deviceid, mode){

    /* get from database the key*/
    connection.connect(function (err) {
        console.log("wew");
    });

    /* split it to key */

    /* and give return back to as key with array */

    return Key_Asek;
}